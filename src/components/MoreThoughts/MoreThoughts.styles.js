/* eslint-disable linebreak-style */
import styled from 'styled-components/macro'

export const StyledMoreThoughts = styled.div`
    text-align: center;

    p {
        font-family: 'Roboto';
        line-height: 1.81em;
        font-style: normal;
        font-weight: 400;
        letter-spacing: 0.01em;
        color: black;
        font-size: 1.1em;
    }

    a {
        text-decoration: none;

        &:hover {
            text-decoration: underline;
        }
    }

`