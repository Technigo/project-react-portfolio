import React from 'react';
// import styled from 'styled-components';
import { BlockSection, InnerWrapper } from './StyledComponents/Wrappers';

export const More = () => {
  return (
    <BlockSection>
      <InnerWrapper>More</InnerWrapper>
    </BlockSection>
  );
};
