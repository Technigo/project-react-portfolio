import React from 'react';
import { StyledHeader } from 'StyledComponents/Header.style';
import GlobalStyle from 'StyledComponents/GlobalStyle';

const Header = () => {
	return <StyledHeader>Testing styled component</StyledHeader>;
};

export default Header;
